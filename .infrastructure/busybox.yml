apiVersion: v1
kind: Pod
metadata:
  name: busybox
  namespace: todoapp
spec:
  containers:
  - name: busybox
    image: curlimages/curl:8.6.0  # Це "золотий стандарт" для тестів curl
    command: ["sh", "-c", "sleep 3600"]
